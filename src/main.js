import { MainController } from "./controllers/MainController.js";

document.addEventListener("DOMContentLoaded", () => {
  console.log("Menjalankan GrafikaApp (Strict MVC)...");
  const app = new MainController();
});
